// Init Effects

import { forward } from 'effector';
import { $stage, updateCurrentIDandStage } from './index';

/* forward({
  from: updateCurrentIDandStage.map(({ ID }) => ID),
  to: $stage,
}); */
